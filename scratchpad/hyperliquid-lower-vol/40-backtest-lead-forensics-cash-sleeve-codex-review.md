## Review findings

| Severity | Cell(s) | Finding and fix |
|---|---:|---|
| Material | 48, 50, 0 | The unlimited floor family has the wrong un-floored comparator. `thr150_nallcap_f*` retains the 33% cap, but its floor chain uses `thr150_nall_invvar`, which explicitly has `max_concentration_pct=1.0`. The “off” column in the heading even labels that comparator “cap off”. Therefore the unlimited-floor axis and its plateau verdicts are not like-for-like. This does not overturn the direct gate-7 failures of those floor runs, but it invalidates the claimed comparison with the un-floored capped book. Fix: use only a no-floor run with the identical capped unlimited parameters, or mark that axis/comparison unevaluable. |
| Material | 39–40, 0 | `band_attribution()` identifies addresses that were in the threshold band on at least one date, then attributes their *entire-window* position P&L. It does not measure P&L earned during dates when the tighter threshold excluded them. Thus “the threshold cliff is that position” and the 71.5% attribution are not causal evidence for the threshold difference; they are full-window address associations. Fix: aggregate position-P&L changes only over the aligned decision intervals in which the wide run held the address and the tight run excluded it; soften the heading meanwhile to “consistent with”. |
| Material | 50 | The uncapped threshold N-chain is malformed at `thr150_n5`: its upper neighbour is `nocap`, which removes the threshold as well as changing N from 5 to 6. The correct six-name neighbour would need the same 1.5 threshold and uncapped sizing. Its current plateau result is therefore not a one-axis N test. It still fails against `thr150_n4`, so the reported rejection happens to stand. Fix: do not score that plateau against `nocap`; require a parameter-matched neighbour. |
| Material | 19, 50 | `plateau_gate()` passes with any non-empty neighbour list, while the standing rule requires both pre-registered neighbours. Boundary cases such as `n3cap`, `anchor_f30`, and other endpoints are therefore technically eligible to pass a one-sided plateau. None passes here, but the gate implementation is wrong. Fix: require exactly two valid neighbours for a plateau verdict; report boundary rows as unevaluable diagnostics. |
| Material | 43–44, 0 | The quality-population table is not drawn from the population to which the `thr150` floor is applied. `CRASH_LOG["candidate_addresses"]` is logged before the threshold filter, whereas the quality floor runs after it. The table reports roughly 148 candidates while `thr150` passes roughly 136 survivors to the floor. Consequently the claims about floor placement “over the same candidate sets” and the qualifying-population calibration are inaccurate for the thresholded floor family. Fix: record/use post-crash survivor addresses, or reconstruct them from `candidate_addresses - excluded_addresses`, before measuring quality coverage and clear counts. |
| Material | 0, supported by 34, 36, 40, 42 | Several interpretations exceed the evidence, although their underlying numbers are correct. “Every six-name lead is the anchor’s book with one position moved”, “the threshold cliff is that position”, and “N = 4 is one vault at twice the weight” are stronger than the displayed evidence. `thr100`, for example, differs on 51.6% of decisions, and the threshold band contains eight addresses. The four-name books average about 3.9 holdings, not two. Fix: describe these as high overlap/concentration findings, and reserve causal claims for temporally aligned attribution. |
| Minor | 0, 34 | The heading says `thr100` has no `0x77fe..1a16` position after 4 June, citing cell 34. Cell 34 displays only the eight largest absolute-P&L positions, so it cannot establish absence of later small positions. Fix: display or assert all positions for that address before making the absence claim. |
| Minor | 0, 48 | “The sleeve works” is stronger than the full-run evidence. The full notebook logs `fill` using the same formula being claimed, so that portion is tautological; the independent cap check is only in the short smoke test and permits tolerance. The smoke test is useful evidence, but the heading should scope the assertion to it. Fix: say the smoke test verified the sleeve invariants; describe full-window results as observed deployment rather than a fresh proof. |
| Minor | 0 | “NB39’s rank correlation … is real and does not select” joins an upstream correlation with an untested ranker counterfactual. This notebook tests a floor, not ranking by `quality_sharpe`; the statement that a ranker “would fill the book” with the floor survivors is not established here. Fix: limit the conclusion to failure of this floor-and-sleeve construction. |

## Sections that check out

- Central return, volatility and Sharpe metrics use the two-day cycle clock, not zero-filled daily returns (cell 16). Sample standard deviation is used for cycle volatility.
- `quality_sharpe` implements the stated 180-row log-return ratio; forward-filled marks add zero to both sums. Its freshness, recency, span and age guards are causal when read through the strategy’s T−1 indicator interface (cells 10 and 14).
- The floor is placed after the crash filter and before ranking. An empty qualifying set is allowed through to the alpha model, and the smoke-test/result behaviour is consistent with open positions being closed rather than retained.
- The sleeve scaling arithmetic is sound: scaling investable equity by `k/S` and widening the normalisation cap by `S/k` preserves the intended full-equity cap for each selected name.
- `forward_log_return()` correctly starts from the last mark at or before T−1, matching the decision-time convention. The drawdown and position-ledger calculations are internally consistent with their stated statistics.
- Anchor parity and lead reproduction are properly asserted, and the reported numerical tables match their cited executed cells.

## Overall verdict

**Not ready to sign off as written.** The headline that none of the floor runs is rescued is supported by their direct standing-gate failures, especially gate 7. However, the unlimited-floor comparison, one N-axis plateau, and the threshold-cliff forensic interpretation are materially unsound. The conclusion should be narrowed until those issues are corrected.