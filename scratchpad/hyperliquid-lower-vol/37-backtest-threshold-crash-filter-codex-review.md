Review of [NB37](</Users/moo/code/getting-started/scratchpad/hyperliquid-lower-vol/37-backtest-threshold-crash-filter.ipynb>):

- **Material — cells 23, 33; heading cell 0.** `standing_gates()` omits gate 2 when it was not run, and gate 6 when no neighbours were supplied, then can emit “PASSES SCORED GATES” or “NOT CONFIRMED”. Those are not valid standing-gate verdicts: an unrun required gate is unevaluated, not passed. This affects, for example, `nocap`, `thr150_nocap`, and `thr150_cagr_sortino__inverse_vol`. Fix: retain an explicit `UNEVALUATED`/`EXPLORATORY` verdict whenever any standing gate is unrun; reserve “NOT CONFIRMED” for runs that passed all five standing gates.

- **Material — cells 29 and 14; heading cell 0.** “Unlimited” does not mean “every survivor … held” for inverse-variance sizing. `max_assets_in_portfolio=999` is only an upper bound. `normalise_weights()` allocates sequentially subject to TVL caps until capital is exhausted; low-weight tails receive zero/uneconomic targets, and the 0.5% close epsilon and USD trade thresholds further prevent persistence. The reported inverse-variance book holds about 25 names, versus 135 equal-weight names. Fix: describe it as an uncapped candidate set with endogenous capital allocation, not an every-survivor portfolio; report selected candidates, non-zero targets, and realised holdings separately.

- **Material — cells 29, 31, 43; heading cell 0 finding 4.** The claim that equal weighting “is holding the junk” is causal and unsupported. The equal run has 135 holdings and 2,560 trades versus 716 inverse-variance trades, with 10% profit redemption fees and rebalance thresholds. The notebook does not decompose the loss between constituent selection, turnover, fees, and residual-size handling; cell 43 audits only the anchor. Fix: say only that equal weighting produced a negative realised result under this implementation; remove the asserted cause unless supported by the existing per-run fee/trade accounting.

- **Material — cell 27; heading cell 0 finding 1.** `thr150` is not “`measured_8` restated”. `measured_8` excludes exactly eight finite candidates per decision and previously had Sharpe 2.374; `thr150` excludes a variable 4–24 candidates (mean 12.48) and has Sharpe 2.294. Fix: call it a threshold-filter analogue, not a restatement.

- **Material — cells 29 and 33; heading cell 0 finding 3.** “Every N < 6 run fails at least two standing gates” is false. `thr100_n3` and `thr100_n4` show only gate 6 failed among scored gates; gate 2 was not run. Likewise, “fewer names is worse at every N” conflicts with the stated outcome: `thr150_n4` has the notebook’s highest CAGR and Sharpe, though it fails gate 3 and gate 6. Fix: distinguish raw outcome from standing-gate status, and say these runs are rejected or unevaluated rather than universally worse.

- **Material — cell 31; heading cell 0 finding 5.** Two comparative claims are false. Equal weighting does not cost CAGR under every ranker: for `cagr_downside`, equal-weight CAGR is 14.8% versus 9.2% inverse variance; for `calm_score`, it is also less negative. `inverse_vol` is not worse than inverse variance everywhere either: it beats it on both CAGR and Sharpe for `cagr_downside`. Fix: replace universal claims with the exact comparisons supported by the table.

- **Minor — cells 23 and 33.** The `scored` comprehension treats `gate_3_dates` as a gate merely because its name starts with `gate_`. It happens to be truthy here, so it does not change the reported results, but it corrupts `standing_gates_scored`. Fix: use an explicit tuple of Boolean gate fields.

- **Minor — cells 25 and 27; heading cell 0.** The heading says threshold 2.5 is “bit-identical … on every cycle”, but cell 27 does not display the `inert` Boolean or an exact cycle-return comparison; it only displays rounded metrics and zero basket changes. Fix: display the exact index match, maximum return difference, and `inert` result before citing the claim.

- **Minor — heading cell 0 finding 1.** “Halves volatility” and “halves drawdown” are inaccurate: 0.0977 versus 0.1543 is a 37% volatility reduction; −2.97% versus −4.45% is a 33% shallower maximum drawdown. Fix the wording.

What checks out: cell 14’s threshold conversion (`sqrt(365) / inverse_vol`), strict `>` boundary, permissive non-finite handling, and held-versus-unheld hysteresis are correctly implemented. The anchor parity result in cell 25 is sound, and the strategy-level Sharpe/volatility metrics use the two-day cycle clock. The exploratory centre selection is conspicuously disclosed.

Overall verdict: **material reporting and verdict-logic corrections required**. The raw backtests and splice appear mechanically sound, but the unlimited-book interpretation and several headline conclusions currently overstate what the executed evidence establishes.