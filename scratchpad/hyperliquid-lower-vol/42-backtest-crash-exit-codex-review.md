## Overall verdict

No blocking error overturns the core observed results: both anchor sells are correctly shown as open-valued partial catches, and `gate12_2d` and `anchor_1d` underperform on the reported Sharpe measures. However, the explanatory churn/fire-count claims are not reliable as written, and the conclusion overreaches the single-window evidence. I would require the material items below before approving NB42’s interpretation.

| Severity | Cell | Finding and concrete fix |
|---|---:|---|
| Material | 25, 39 | `_RANK_CACHE` is keyed only by timestamp. Cell 30 has already populated it using `POOL_SOURCE = "pool_2d"`; cell 39 switches to `pool_1d` before calling `churn_pnl("anchor_1d")`, but cached dates still use the two-day pool. Thus the one-day “outranked” count and P&L can be a mixed-clock reconstruction. Fix: key the cache by `(pool_label, timestamp)` or pass the pool label explicitly through `trade_ledger()`/`ranking_at()`, clear the cache, rerun the churn calculations and amend the heading. |
| Material | 25, 39 | Even without the cache defect, `exit_reason()` reconstructs “outranked” from a raw rank and does not reproduce deposit-window skips, minimum-hold protection, or the actual selected set. The module itself documents this limitation. Therefore `churn_pnl` is not a verified rank-churn attribution, and “doubling decisions … turns it into a -$12.5k loss” is unsupported. Fix: log pre-trade selected names and each pool-removal/rank displacement reason inside `decide_trades`, then classify exits from that log. The one-day Sharpe failure remains valid independently. |
| Material | 26, 32 | `gate_fire_count()` derives holdings from `_position_weights`, i.e. post-decision position statistics, not the holdings immediately before the decision. The 2026-01-01 Citadel row proves the error: the strategy began that day with cash, yet the newly opened 32% position is counted as a vault that a tighter gate “would sell”. The 13-fire count and the “four winners” explanation are therefore not the stated pre-decision counterfactual. Fix: log the held set before trade generation, or reconstruct strict pre-decision membership (`opened_at < t` and not closed before `t`) with prior-cycle weights; then regenerate cell 32 and the heading. |
| Material | 0 | “The -16% threshold is … the width that lets the incumbent’s winners survive their own noise” is a causal story, not a result established by this notebook. The stricter run changes future holdings, re-entries, rankings and trades; the anchor-book local fire table cannot attribute its weaker full-book result to those rows. Likewise, “the exit side … is closed as a lead” is broader than the tested two-day threshold tightening, while the cluster variant was not run. Fix: say the pre-registered `-12%` variant produced no lead on this in-sample window; do not generalise to all exit mechanisms or name a mechanism as the cause. |
| Minor | 26 | The fill predicate records `market_feed_delay` but never makes a non-zero delay fail closed. The claimed rationale is false in general: `candle_row()` reads forward-filled candles, so a delayed price can still equal that row’s open at 1e-9. This did not affect the two displayed trades, whose delays are explicitly zero in cell 34. Fix: before assigning a catch label, require a finite zero delay; otherwise label `UNCLASSIFIED` (or compare with a preserved raw-mark row). |
| Minor | 0, 31 | “A 4-hour clock is physically possible on the dense period” is too broad. Cell 31 reports a dense-period maximum empty-bucket share of 81.8% across held vaults. The two collapse paths are well covered, but the output does not establish a usable four-hour universe for the whole book. Fix: restrict the wording to the two collapse vaults / adequately covered dense-series subset. |

What checks out:

- Cell 34 uses `planned_mid_price`, not fee-dirty `executed_price`, against the decision-date candle `open`; the non-async branch and the PARTIAL CATCH labels match the plan. The displayed zero feed delays make the two actual labels valid.
- Cell 30’s raw paths and cell 34’s candle rows support the H0b result: for both vaults, the day’s open equals the previous close and the loss is in the open-to-close leg. The converter’s first/last-mark OHLC construction is consistent with this.
- The collapse positions are selected reproducibly by the stated date-and-momentum-gate rule, with one matching sell each.
- `gate_fire_count()` and `breaker_fire_count()` read their indicators at T-1; the interval `(lo, hi]` is implemented correctly. The defect is the definition of “held”, not the signal read.
- `sharpe_on_2d_grid()` correctly reindexes the one-day equity to the anchor timestamps, asserts no missing observations, and annualises the resulting two-day returns at 182.5 periods/year.
- The first-strike classes are disjoint, use three returns strictly before `t`, and the heading honestly labels their intervals descriptive. The appendix’s `first_extreme` mean does include aftermath observations, as stated.
- The stop rules were applied correctly: H1 fails and run 2 caught the August position, so neither one-day gate nor cluster run was made.
- The principal heading numbers and cited cells match the shown outputs. The live-fill limitation is correctly stated; the notebook quantifies only that the first archived mark was unchanged on these two dates, not a general live-redemption slippage distribution.