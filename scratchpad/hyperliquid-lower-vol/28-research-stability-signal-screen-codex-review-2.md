## Review outcome

The first review’s bootstrap, p-value, paired-sample, and critical-value findings were addressed correctly. However, I would still block acceptance of the gate-5 rejection result as a valid pre-registered screen.

| Severity | Cell | Finding and concrete fix |
|---|---:|---|
| **Blocking** | 20, 26, 29–30; heading cell 0 | The all-zero event-concentration result is still merely observed, not shown unreachable. There is no calibration/reachability result showing this target and max-T procedure can clear the rule on this cohort. The heading appropriately says “failure to establish”, but still uses the result to reject every signal at gate 5. | Add a reachability demonstration to the screen, or restrict the verdict to “none passed this implementation”. Do not use it as a decisive rejection gate until reachability is established. |
| **Blocking** | 20 | `forward_event_top5_excess = share - 5/n` removes the algebraic floor only for equal positive events; it does not remove reporting-count dependence. The expected top-five share of unequal random positive returns remains a function of `n`, and the attainable range changes from `0..0.375` at `n=8` to `0..0.9` at `n=50`. Thus it is not a count-neutral concentration target as described. | Treat this revised target as count-confounded. A future pre-registered target would need conditional-on-`n` standardisation or a fixed-count construction; this run’s conclusion must remain diagnostic. |
| **Material** | 0, 20 | The heading calls the excess target’s result “under the pre-registered criterion”, while also admitting that “the excess-over-uniform construction is new to this run”. It was introduced after the first run and after seeing the original defect/result. This changes the gate-5 estimand, not merely its presentation. | Label the excess-target screen as a post-review diagnostic/correction, not as the pre-registered gate-5 verdict. Freeze it prospectively before using it for a gate. |
| **Material** | 0; `_build/write_heading_28.py:206–207` | Provenance in the generated heading is false. It says `vault-prices.parquet` was 254,818,366 bytes with hash `3e79966a`; cell 22 reports 255,136,297 bytes and `8bce13191183c7d4`. The generator hard-codes the old values. | Put provenance output in the manifest and render it from that manifest, as done for the numerical screen results. |
| **Material** | 0; `_build/write_heading_28.py:94–97` | “On 110 eligible decisions and 8,312 complete-case rows, 5 signals clear…” assigns `inverse_vol`’s sample to a five-signal result. The five signals have different samples: e.g. `ulcer_index_180` has 84 dates / 2,681 rows and `positive_window_share` 106 / 5,401. | State sample sizes per signal, or say that each signal’s own sample is shown in cell 29. |
| **Minor** | 0, paragraph 3; cites cell 32 | The return-contrast construction is correct for the stated compounded-log-return estimand, and the heading is broadly honest that it is not an achievable rate. But “the excluded tail collects” the near-total-loss observations is not established by cell 32: it shows only the overall target distribution and contrasts, not tail membership for each signal. | Say only that extreme observations make this compounded contrast unstable, unless group membership is displayed. |

The corrected complete-family max-T handling, finite-draw add-one p-values, corrected return-Spearman orientation, common-sample calendar/fresh comparison, and current critical values are all sound. The cycle-clock portfolio metrics and T−1 signal access also look correct.

So the numerical statement “0 of 13 passed this revised implementation” is reproduced, but it should not be treated as a conclusive, pre-registered gate-5 rejection of all thirteen signals.